# proposed-references (DRAFT — not yet integrated)

These 13 references are the **next field tranche**: lessons distilled from
extended multi-agent work across the three benches, written in the same format
as the integrated `references/` (trigger -> Core Rule -> rules -> Acceptance ->
Stop Rules -> Evidence Provenance).

**Status: DRAFT, verified by two-lane cross-review, but NOT yet wired into the
`SKILL.md` funnel.** They are published here for transparency and review, kept
separate from the verified `references/` on purpose — verified and draft must not
be blurred. Integration (merging or linking each into the funnel, deciding final
granularity, and re-running the skill's own integrity tests) is the remaining
step.

| File | Covers |
|---|---|
| `purpose-canon-and-source-authority.md` | Operator-language purpose canon, asset recoverability, source authority rank, governance as read-op. |
| `surface-census-atlas-natural-questions.md` | Surface census, doctor parity, generated command atlas, natural-question replay. |
| `write-ladder-red-max.md` | GREEN/YELLOW/RED tiers, RED-max fresh confirmation, the absurdity test, hold-first precedence. |
| `apply-classifier-envelope-readback.md` | Apply-outcome classifier, envelope honesty, host echo of effective params, readback sized to risk. |
| `schema-allowlist-row-identity-rule-binding.md` | Schema no wider than the app, row identity, rule-to-capability binding. |
| `backup-restore-delta.md` | Back up before destroy, abort on irreplaceable, rehearsal as acceptance, post-backup delta replay. |
| `artifact-manifest-and-import-corridors.md` | Artifact manifest, preview != apply, deterministic-first parsing, provider tiers. |
| `text-comment-semantic-freshness.md` | Edit-grade reads, byte-level diff, negated-token tests, freshness semaphore. |
| `transport-json-windows-cloudsync.md` | PowerShell carriers, numeric typing across boundaries, mojibake by byte structure, authoritative surface. |
| `dynamic-ai-registry-cost-guard.md` | Dynamic model registry, cost guard before expensive calls, secrets as booleans. |
| `multi-agent-worksite-and-charter.md` | Worksite scaffold, post-restart reconciliation, content-blind router, conditional charter. |
| `operator-visual-acceptance-ux.md` | Operator-assisted probes, dense-UI discipline, no raw enums on screen. |
| `release-claim-canaries-closure.md` | Canary invariants, greppable closure labels, imputation ladder, dual-mandate promotion gate. |

Each file carries its own claim-limit: this is method guidance, not proof of any
deployed bridge.
